spring:
  application:
    name: springcopilotonl
  datasource:
    url: jdbc:postgresql://localhost:5432/lincopilot_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
